language: node_js
cache:
  directories:
    - node_modules
notifications:
  email: false
jobs:
  include:
    - stage: test
      node_js: "9"
      script:
        - npm test
    - stage: coverage
      node_js: "9"
      script:
        - npm run test:coverage
    - stage: lint
      node_js: "9"
      script:
        - npm run lint:prettier
        - npm run lint:js
